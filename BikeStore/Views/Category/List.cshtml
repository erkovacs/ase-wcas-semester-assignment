@model IEnumerable<Category>
@{
    ViewData["Title"] = "Categories";
}

    <h2>Categories</h2>

@foreach (var p in Model)
{
    <div>
        <h3>@p.Name</h3>
        @p.Description
        <br />
        <a href="/Category/Products/@p.CategoryID">See products</a>
    </div>
}
